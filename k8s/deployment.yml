apiVersion: apps/v1
kind: Deployment
metadata:
  namespace: notes-app
  name: notes-app-deployment
  labels:
    app: notes-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: notes-app
  template:
    metadata:
      labels:
        app: notes-app
    spec:
      containers:
      - name: notes-app
        image: itskdm14/notes-app-k8s
        ports:
        - containerPort: 8000

