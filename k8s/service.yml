kind: Service
apiVersion: v1
metadata:
  namespace: notes-app
  name: notes-app-service
spec:
  selector:
    app: notes-app
  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000
  type: ClusterIP
  
